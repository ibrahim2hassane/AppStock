{% extends 'accueil.html.twig' %}

{% block title %}Gestion des Utilisateurs{% endblock %}

{% block body %}
    <div class=" container row">
        <div class="container col-md-6">
            <div class="card">
                <div class="card-header text-center "> Liste des Utilisateurs </div>
                    <div class="card-body">
                        <table class="table table-striped">
                             <thead>
                                <tr>
                                    <th>Identifiant </th>
                                    <th>Email</th>
                                    <th>Nom</th>
                                    <th>Prénom</th>
                                    
                                </tr> 
                                {% for u in users %}
                                    <tr> 
                                        <td>{{ u.id}} </td>
                                        <td>{{ u.email}} </td>
                                        <td>{{ u.nom}} </td>
                                        <td>{{ u.prenom}} </td>
                                        
                                    </tr>
                                {% endfor %}
                                </thead>
                        </table>
                    </div>
            </div>
        </div>
            <div class="container col-md-6">
                    <div class="card ">
                        <div class="card-header"> Formulaire de Gestion des Utilisateurs </div>
                            <div class="card-body"> 
                                {{ form(form)}}
                            </div>
                    </div>
            </div> 
    </div>
{% endblock %}
